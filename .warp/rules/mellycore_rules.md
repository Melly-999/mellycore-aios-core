# MellyCore Warp Rules

- Always confirm repo path and branch.
- Never run reset, clean, rebase, force push, branch deletion, or deploy commands without explicit approval.
- Never echo secrets.
- Never edit `.env` values.
- Never commit provider tokens, account IDs, databases, or local runtime state.
- Update `shared_context/AGENT_HANDOFF.md` after meaningful work.

