# GLM Workspace Inventory

The path `C:\AI\MellyCore_Workspace\03_Assets\glm_workspace_reference` is a GLM/Z.ai generated workspace reference only.

Expected reference value:

- Likely Next/Tailwind/shadcn/Bun CSS effects prototype.
- Useful design and effects reference.
- Purple/blue orbital cube visual direction.
- Future frontend inspiration.

Safety:

- Do not commit or import the reference workspace wholesale.
- Do not import its `.git` folder.
- Do not import `.env`.
- Do not import `db/custom.db`.
- Do not copy secrets, local runtime state, or database files.
- The reference path is outside the repo.

Bootstrap inventory result:

- The reference directory existed during bootstrap.
- No safe high-level child files or folders were visible during the initial filename-only inspection.
- If files appear later, inspect names only first and document safe high-level folders/files without reading `.env` contents.

